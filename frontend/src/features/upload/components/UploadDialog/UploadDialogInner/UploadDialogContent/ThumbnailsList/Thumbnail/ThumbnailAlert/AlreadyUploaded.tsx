import { FormattedMessage } from 'react-intl';

export function AlreadyUploaded() {
  return (
    <FormattedMessage
      defaultMessage="This photo has been previously uploaded"
      description="upload thumbnail error already uploaded"
    />
  );
}
