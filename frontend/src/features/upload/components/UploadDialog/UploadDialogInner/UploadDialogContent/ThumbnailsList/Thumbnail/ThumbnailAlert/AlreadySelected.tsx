import { FormattedMessage } from 'react-intl';

export function AlreadySelected() {
  return (
    <FormattedMessage
      defaultMessage="This photo has already been selected"
      description="upload thumbnail error already selected"
    />
  );
}
